# Requirements для Dynamic vLLM Server
# PDF Converter Pipeline v2.0 - поддержка динамической подгрузки/выгрузки моделей

# Основной vLLM (если не установлен в базовом образе)
vllm>=0.6.0
torch>=2.1.0
transformers>=4.36.0

# FastAPI для REST API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# HTTP клиенты и утилиты
httpx>=0.25.0
requests>=2.31.0
aiohttp>=3.9.0

# Системные утилиты
psutil>=5.9.0
pynvml>=11.5.0
nvidia-ml-py>=12.0.0

# Логирование и мониторинг
structlog>=23.2.0
prometheus-client>=0.19.0

# Дополнительные утилиты для Model Manager
asyncio-throttle>=1.0.0
tenacity>=8.2.0
cachetools>=5.3.0

# Для работы с GPU памятью и оптимизацией
gpustat>=1.1.0
py3nvml>=0.2.7

# JSON и конфигурационные файлы
pyyaml>=6.0.0
toml>=0.10.0

# Математические и научные библиотеки (если нужны)
numpy>=1.24.0
scipy>=1.11.0

# Дата и время
python-dateutil>=2.8.0

# Для отладки и разработки
rich>=13.0.0
click>=8.0.0

# Безопасность
cryptography>=41.0.0

# Дополнительные оптимизации
flash-attn>=2.3.0; platform_machine == "x86_64"
xformers>=0.0.22; platform_machine == "x86_64"

# Поддержка специфичных моделей Qwen
tokenizers>=0.15.0
sentencepiece>=0.1.99
protobuf>=4.24.0

# Для обработки изображений (если используется Qwen2.5-VL)
pillow>=10.0.0
opencv-python>=4.8.0

# Асинхронные операции
asyncio>=3.4.3
aiofiles>=23.2.0

# Метрики и телеметрия
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0